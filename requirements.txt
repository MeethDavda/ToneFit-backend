# --- core web ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
gunicorn==22.0.0
python-multipart==0.0.9
starlette==0.38.5

# --- image / ML deps (NumPy 1.x line to avoid ABI issues) ---
numpy==1.26.4
opencv-python-headless==4.9.0.80
pillow==10.3.0
onnxruntime==1.17.3
rembg==2.0.57

# --- optional analytics (used by your color rules) ---
scikit-learn==1.4.2
scipy==1.11.4

# (remove if unused)
# scikit-image  # <— you’re not using it; leave out to save RAM
# numba         # heavy; leave out unless you really need it
